<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liuyuesi.demo.mapper.VehicleTypeImageMapper">
    
   <!--<insert id="insert" parameterType="com.liuyuesi.demo.entity.VehicleTypeImage">
    	insert ignore into vehicletypeimage(vehicle_type_id, image_name,image_url)
    	values (#{typeId}, #{imageName}, #{imageUrl}) 

    	<selectKey keyProperty="typeId" order="AFTER" resultType="byte">
        	SELECT LAST_INSERT_ID()
    	</selectKey>
	</insert> -->
 
</mapper>